#include <stdio.h>

int main(){
    float a,b;
    printf("Enter base: ");
    scanf("%f", &a);
    printf("Enter height: ");
    scanf("%f", &b);
    printf("The area of the triangle = %.1f", .5*a*b );
    return 0;
}
