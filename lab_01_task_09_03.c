#include <stdio.h>

int main(){
    float a,b;
    printf("Enter length: ");
    scanf("%f", &a);
    printf("Enter length: ");
    scanf("%f", &b);
    printf("The area of the square = %.1f", .5*a*b );
    return 0;
}